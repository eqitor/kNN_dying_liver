# kNN_dying_liver
This notebook shows method of classification using k-nn algorithm. Classifier will be evaluated using different hyperparameters (different metrics, k value and number of attributes). The experiment was performed using scikit-learn, numpy and pandas.
